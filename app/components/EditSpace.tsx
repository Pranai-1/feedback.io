import { SetStateAction } from "react";
import { SpaceCreationProviderProps, SpacePropType } from "../api/types";

export default function EditSpace({setEditSpace,space}:
    {setEditSpace:React.Dispatch<SetStateAction<string>>,space:SpacePropType}){
    return(
        <></>
    )
}